#!/usr/bin/env bash
set -euo pipefail

# Build the rootfs ext4 image for the python language pack.
# Target output: ./python.ext4 (Alpine base + Python 3 + emberd-init as PID 1).

echo "rootfs build not yet implemented"
exit 1
